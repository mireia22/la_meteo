import styled from "styled-components";

export const Select = styled.select`
  padding: 0.2rem;
  background: transparent;
  border: 2px solid var(--warm-dark);
  border-radius: 5px;
  color: var(--warm-dark);
  outline: none;
  font-weight: bold;
`;

export const Option = styled.option`
  background: rgba(189, 43, 58, 0.8);
  color: var(--font);
  font-weight: bold;
`;
